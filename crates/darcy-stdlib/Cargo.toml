[package]
name = "darcy-stdlib"
version = "0.3.1"
edition = "2021"

[dependencies]
indexmap = "2"

[features]
darcy-compiled = []
default = ["darcy-compiled"]

[build-dependencies]
darcy-build = { path = "../darcy-build" }
