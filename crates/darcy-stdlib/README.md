This crate is the canonical Darcy stdlib project.

Source lives under `crates/darcy-stdlib/darcy/`.
Generated Rust is emitted into `OUT_DIR` when the `darcy-compiled` feature is enabled
(default on).
