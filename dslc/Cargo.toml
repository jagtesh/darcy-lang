[package]
name = "dslc"
version = "0.1.0"
edition = "2021"

[dependencies]
cel = { version = "0.12.0", optional = true }
serde_json = "1.0"

[features]
# Enable CEL comparison by wiring a CEL evaluator in dslc/src/bin/bench.rs.
cel = ["dep:cel"]
